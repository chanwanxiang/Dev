- baseinfo:
      apiName: 接单接口
      path: /api/order/pc/order/master/receive
      method: post
      headers:
        ContentType: application/json;charset=UTF-8
      cookies:
        ${get_extract_data(Cookie)}
  testCase:
    - caseName: 接单
      json:
        orderId: ${get_extract_data(orderId)}
      validation:
        - contains: { 'message': '操作成功' }
