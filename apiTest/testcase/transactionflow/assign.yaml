- baseinfo:
      apiName: 分配物流接口
      path: /api/order/pc/order/assign
      method: post
      headers:
        ContentType: application/json;charset=UTF-8
      cookies:
        ${dealextractYaml(cookies)}
  testCase:
    - caseName: 分配物流
      json:
        orderId: ${dealextractYaml(orderId)}
        orgId: '4140913758110176843'
      validation:
        - contains: { 'message': '操作成功' }